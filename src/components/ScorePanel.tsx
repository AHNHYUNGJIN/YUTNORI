import type { PawnState, PlayerState } from '../domain/types';
export function ScorePanel({players,pawns}:{players:PlayerState[];pawns:PawnState[]}){return <aside className="rounded-3xl bg-white p-4 shadow-lg dark:bg-slate-800"><h2 className="mb-3 text-xl font-black">가족 점수판</h2><div className="space-y-2">{players.map(pl=>{const home=pawns.filter(p=>p.playerId===pl.id&&p.position==='HOME').length;return <div key={pl.id} className="flex items-center justify-between rounded-2xl p-3 font-bold" style={{backgroundColor:`${pl.color}22`}}><span>{pl.name}</span><span>{home}/4 도착</span></div>})}</div></aside>}
